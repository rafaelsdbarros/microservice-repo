spring:
  datasource:
    password: root
    username: root
    url: jdbc:mysql://localhost:3306/fornecedor-paper?serverTimezone=UTC&useLegacyDatetimeCode=false
  jpa:
    hibernate:
      ddl-auto: update
