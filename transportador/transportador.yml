spring:
  datasource:
    password: root
    username: root
    url: jdbc:mysql://localhost:3307/transportador?serverTimezone=UTC&useLegacyDatetimeCode=false
  jpa:
    hibernate:
      ddl-auto: update
